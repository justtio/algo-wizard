## 二叉树的层序遍历

给你二叉树的根节点 root ，返回其节点值的 层序遍历 。 （即逐层地，从左到右访问所有节点）。

```
示例 1：
输入：root = [3,9,20,null,null,15,7]
输出：[[3],[9,20],[15,7]]

示例 2：
输入：root = [1]
输出：[[1]]

示例 3：
输入：root = []
输出：[]
 

提示：
树中节点数目在范围 [0, 2000] 内
-1000 <= Node.val <= 1000
```

核心思路：我们用一个队列来储存每一层待处理的节点。

```js
/**
 * 定义二叉树节点
 * function TreeNode(val, left, right) {
 *  this.val = val === undefined ? 0 : val;
 *  this.left = left === undefined ? null : left;
 *  this.right = right === undefined ? null : right;
 * }
 */

var levelOrder = function(root) {
  //如果树为空，直接返回空数组
  if(!root) return [];

  //存放每一层的节点值
  const result = [];
  //存放待处理的节点，初始放入root节点
  const queue = [root];

  //当队列不为空时，循环继续
  while(queue.length > 0) {
    //获取当前层的节点数量
    const levelSize = queue.length;
    //创建一个数组用来存放当前层的节点值
    const currentLevel = [];

    //遍历当前层的每一个节点
    for(let i = 0; i < levelSize; i++) {
      //从队列头部取出一个节点
      const node = queue.shift();
      //将节点值放入当前层数组
      currentLevel.push(node.val);

      //如果有左子节点，将其放入队列
      if(node.left) {
        queue.push(node.left);
      }
      //如果有右节点，将其放入队列
      if(node.right) {
        queue.push(node.right);
      }
    }
    //当for循环结束，意味着当前层的所有节点都处理完成
    //将当前层数组放入结果数组中
    resulst.push(currentLevel)
  }
  return result;
}
```